"""claude-mirror — sync Claude project MD files across machines via cloud storage.

The package version is the single source of truth in ``pyproject.toml``;
import it at runtime via ``importlib.metadata.version("claude-mirror")``
rather than relying on a hardcoded constant here.
"""
